@model IEnumerable<SmartEyeClinic.Models.Specialization>
@{
    ViewData["Title"] = "Specializations - Smart Eye Clinic";
}

<div class="page-header">
    <div class="page-header-left">
        <nav class="breadcrumb-nav">
            <a asp-controller="Home" asp-action="Index">Dashboard</a>
            <span class="separator">/</span>
            <span class="current">Specializations</span>
        </nav>
        <h3 class="page-title">Clinical Specializations</h3>
        <p class="page-subtitle">Configure medical departments, specialization tags, and active physicians lists</p>
    </div>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Create" class="btn btn-primary">
            <i class="fa-solid fa-plus"></i> Add Specialization
        </a>
    }
</div>

<div class="row g-4 animate-fade-in-up">
    @foreach (var item in Model)
    {
        <div class="col-xl-4 col-md-6">
            <div class="card custom-card h-100">
                <div class="card-body-clean p-4 d-flex flex-column h-100">
                    <div class="d-flex align-items-center mb-3">
                        <div class="avatar-initials avatar-lg me-3 bg-light-primary text-primary-custom" style="border-radius: var(--radius)">
                            <i class="fa-solid fa-tags fs-4"></i>
                        </div>
                        <div>
                            <h5 class="fw-bold text-dark mb-0">@item.Name</h5>
                            <span class="text-xs text-muted">Dept Code: #@item.Id</span>
                        </div>
                    </div>

                    <p class="text-muted text-xs mb-4 flex-grow-1">
                        @(string.IsNullOrEmpty(item.Description) ? "No department description entered." : item.Description)
                    </p>

                    <hr class="text-muted opacity-10 my-3" />

                    <div class="d-flex justify-content-between align-items-center mt-auto">
                        <div>
                            <span class="text-muted text-xs d-block">Active Specialists</span>
                            <strong class="text-dark font-size-sm">@item.Doctors.Count Doctors</strong>
                        </div>
                        <div class="action-btns">
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-light" title="View details">
                                <i class="fa-solid fa-eye text-muted"></i> Details
                            </a>
                            @if (User.IsInRole("Admin"))
                            {
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-light text-primary" title="Edit department tags">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-custom-danger" title="Delete specialization">
                                    <i class="fa-solid fa-trash-can"></i>
                                </a>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
